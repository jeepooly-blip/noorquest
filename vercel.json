{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "regions": ["iad1"],
  "buildCommand": "cd noorquest && pnpm install --frozen-lockfile=false && pnpm build",
  "installCommand": "cd noorquest && pnpm install --frozen-lockfile=false",
  "outputDirectory": "noorquest/.next",
  "github": {
    "silent": false,
    "autoAlias": true
  }
}
