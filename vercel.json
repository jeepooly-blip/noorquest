{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "rootDirectory": "noorquest",
  "framework": "nextjs",
  "regions": ["iad1"],
  "buildCommand": "pnpm install --frozen-lockfile=false && pnpm build",
  "installCommand": "pnpm install --frozen-lockfile=false",
  "github": {
    "silent": false,
    "autoAlias": true
  }
}
